import {Howl, Howler} from 'howler';

var playPreview = {
    preview(song){
        this.userInteracted = true
        console.log(Howler)
              var sound = new Howl({
                src: (songPreview)
              });
        
              sound.play();
    }
}

export default playPreview